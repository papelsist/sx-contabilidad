import { FormaDePagoPipe } from './forma-de-pago.pipe';

describe('FormaDePagoPipe', () => {
  it('create an instance', () => {
    const pipe = new FormaDePagoPipe();
    expect(pipe).toBeTruthy();
  });
});
